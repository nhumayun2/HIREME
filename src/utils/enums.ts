export enum UserRole {
  ADMIN = "admin",
  EMPLOYEE = "employee",
  JOB_SEEKER = "job_seeker",
}

export enum JobStatus {
  OPEN = "open",
  CLOSED = "closed",
}

export enum ApplicationStatus {
  PENDING = "pending",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
}
